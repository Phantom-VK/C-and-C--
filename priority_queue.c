#include<stdio.h>
#define MAX_SIZE 100;

struct item{

    int value;
    int priority;

}

struct item pr[MAX_SIZE];
int size = -1;

void enqueue(int item, int priority){

    int highestPriority = 0;
    int index = -1;
}

